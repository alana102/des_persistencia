[project]
name = "codigos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deltalake>=1.5.0",
    "fastapi[standard]>=0.135.2",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
]
